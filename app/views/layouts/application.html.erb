<!DOCTYPE html>
<html>
<head>
  <title>SturegicOriginal</title>
  <%= csrf_meta_tags %>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <link href="https://fonts.googleapis.com/css?family=Bellefair" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css?family=Varela+Round" rel="stylesheet">
</head>

<body>
  <nav class="navbar navbar-light myNavbar">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li class="active"><a class="nav-content-format" href="#">KIAC<span class="sr-only">(current)</span></a></li>

        </ul>
        <ul class="nav navbar-nav navbar-right">
            <% if log_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle nav-content-format" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> <div class="glyphicon glyphicon-user">
            <% if current_user %>
              </div><%= current_user.first_name %> <%= current_user.last_name %>
            <% end %>
            <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="#">Bonus Status</a></li>
              <li><a href="/students/<%= @user.friendly_id%>/edit">Settings</a></li>
            </ul>
            <% end %>
          </li>
          <% if current_user %>
          <li><a class="nav-content-format" href="/logout">Logout</a></li>
          <% else %>
          <li><a class="nav-content-format" href="/login">Login</a></li>
          <% end %>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>
    <p>
      <% flash.each do |key, value| %>
      <% alert_class = case key
      when :notice then "success"
      when :alert then "danger"
      else key
        end %>
        <%= content_tag :div, class: "alert alert-#{alert_class}" do %>
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <%= value %>
        <% end %>
        <% end %>
      </p>
    </div>
    <%= yield %>
  </body>
  </html>
