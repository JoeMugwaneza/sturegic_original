<% @user.technicalgroups.each do |group|%>
<h2> Group <%= @user.technicalgroups.index(group) +1 %></h2>

<div class="col-md-offset-2 panel">
  <h2 class="text-center">technicalnical Programs | Summary</h2>

  <table class="table table-hover">
    <thead>
      <th>Level</th>
      <th>Students</th>
      <th>Bonus Unit</th>
      <th>Total Bonus</th>
      <th>Reward</th>
      <th>Time Remaining</th>
      <th>Progress</th>
    </thead>
    <tbody>

      <tr>        
        <td>1</td>
        <td><%= @user.level_one(@user.technicalgroups.index(group),"technical").count %></td>
        <td>5000</td>
        <td><%= "#{@user.level_one(@user.technicalgroups.index(group),"technical").count * 5000}"%></td>
        <td>No Reward</td>
        <td> - </td>
        <td> - </td>
      </tr>
      
      <tr>

        <td>2</td>
        <td><%= @user.level_two(@user.technicalgroups.index(group),"technical").count %></td>
        <td>4000</td>
        <td><%= "#{@user.level_two(@user.technicalgroups.index(group),"technical").count * 4000}"%></td>
        <td>Laptop</td>
        <td>90 days</td>
        <td>          
          <div class="progress">
            <div class="progress-bar progress-bar-info" role="progressbar" aria-valuenow="50"
            aria-valuemin="0" aria-valuemax="100" style="width:<%=(@user.level_two(@user.technicalgroups.index(group),"technical").count * 100)/25%>%">
            <%=(@user.level_two(@user.technicalgroups.index(group),"technical").count * 100)/25%>%</div>
          </div>
        </td>
      </tr>

      <tr>        
        <td>3</td>
        <td><%= @user.level_three(@user.technicalgroups.index(group),"technical").count %></td>
        <td>3000</td>
        <td><%= "#{@user.level_three(@user.technicalgroups.index(group),"technical").count * 3000}"%></td>
        <td>Scholarship</td>
        <td>180 days</td>
        <td>          
          <div class="progress">
            <div class="progress-bar progress-bar-info" role="progressbar" aria-valuenow="50"
            aria-valuemin="0" aria-valuemax="100" style="width:<%=(@user.level_three(@user.technicalgroups.index(group),"technical").count * 100)/25%>%">
            <%=(@user.level_three(@user.technicalgroups.index(group),"technical").count * 100)/125%>%</div>
          </div>
        </td>
      </tr>

      <tr>
        <td>4</td>
        <td><%= @user.level_four(@user.technicalgroups.index(group),"technical").count %></td>
        <td>2000</td>
        <td><%= "#{@user.level_four(@user.technicalgroups.index(group),"technical").count * 2000}"%></td>
        <td>Car</td>
        <td>270 days</td>
        <td>          
          <div class="progress">
            <div class="progress-bar progress-bar-info" role="progressbar" aria-valuenow="50"
            aria-valuemin="0" aria-valuemax="100" style="width:<%=(@user.level_four(@user.technicalgroups.index(group),"technical").count * 100)/125%>%">
            <%=(@user.level_four(@user.technicalgroups.index(group),"technical").count * 100)/625%>%</div>
          </div>
        </td>
      </tr>

      <tr>
        <td>5</td>
        <td><%= @user.level_five(@user.technicalgroups.index(group),"technical").count %></td>
        <td>1000</td>
        <td><%= "#{@user.level_five(@user.technicalgroups.index(group),"technical").count * 1000}"%></td>
        <td>House</td>
        <td>360 days</td>
        <td>          
          <div class="progress">
            <div class="progress-bar progress-bar-info" role="progressbar" aria-valuenow="50"
            aria-valuemin="0" aria-valuemax="100" style="width:<%=(@user.level_five(@user.technicalgroups.index(group),"technical").count * 100)/25%>%">
            <%=(@user.level_five(@user.technicalgroups.index(group),"technical").count * 100)/3125%>%</div>
          </div>
        </td>
      </tr>




    </tbody>
  </table>
</div>

<h2 class="col-md-offset-2 text-center">Students List</h2>



<div class="panel col-md-offset-2">
  <div class="panel-heading" style="background-color: #0b56a8;">
  <h3 class="panel-title" style="color: white;">Levels</h3>
    <span class="pull-right">
      <!-- Tabs -->
      <ul class="nav panel-tabs">
        <li class="active"><a href="#col1TG<%= @user.technicalgroups.index(group) +1 %>" data-toggle="tab">Level 1</a></li>
        <li><a href="#col2TG<%= @user.technicalgroups.index(group) +1 %>" data-toggle="tab">Level 2</a></li>
        <li><a href="#col3TG<%= @user.technicalgroups.index(group) +1 %>" data-toggle="tab">Level 3</a></li>
        <li><a href="#col4TG<%= @user.technicalgroups.index(group) +1 %>" data-toggle="tab">Level 4</a></li>
        <li><a href="#col5TG<%= @user.technicalgroups.index(group) +1 %>" data-toggle="tab">Level 5</a></li>
      </ul>
    </span>
  </div>
  <div class="panel-body">
    <div class="tab-content">
      <div class="tab-pane active" id="col1TG<%= @user.technicalgroups.index(group) +1 %>">
        <table class="table table-hover">
          <thead>
            <tr>
              <th>Full Name</th>
              <th>Program</th>
              <th>District</th>
              <th>Date</th>
            </tr>
          </thead>
          <tbody>
            <% @user.level_one(@user.technicalgroups.index(group), "technical").each do |student| %>
            <tr>
              <td><%= student.first_name %> <%= student.last_name%></td>
              <td><%= student.studentInfo.program_category.name%></td>
              <td>
              <% if student.studentInfo.district %>
              <%= student.studentInfo.district.name%>
              <% else %>
              <%= student.studentInfo.city%>
              <% end %>  
              </td>
              <td><%= student.studentInfo.created_at %></td>
            </tr>
            <% end %>
          </tbody>
        </table>

      </div>

      <div class="tab-pane" id="col2TG<%= @user.technicalgroups.index(group) +1 %>">
        <table class="table table-hover">
          <thead>
            <tr>
              <th>Full Name</th>
              <th>Program</th>
              <th>District</th>
              <th>Date</th>
            </tr>
          </thead>
          <tbody>
            <% @user.level_two(@user.technicalgroups.index(group), "technical").each do |student| %>
            <tr>
              <td><%= student.first_name %> <%= student.last_name%></td>
              <td><%= student.studentInfo.program_category.name%></td>
              <td>
              <% if student.studentInfo.district %>
              <%= student.studentInfo.district.name%>
              <% else %>
              <%= student.studentInfo.city%>
              <% end %>  
              </td>
              <td><%= student.studentInfo.created_at %></td>
            </tr>
            <% end %>
          </tbody>
        </table>

      </div>
      <div class="tab-pane" id="col3TG<%= @user.technicalgroups.index(group) +1 %>">
        <table class="table table-hover">
          <thead>
            <tr>
              <th>Full Name</th>
              <th>Program</th>
              <th>District</th>
              <th>Date</th>
            </tr>
          </thead>
          <tbody>
            <% @user.level_three(@user.technicalgroups.index(group), "technical").each do |student| %>
            <tr>
              <td><%= student.first_name %> <%= student.last_name%></td>
              <td><%= student.studentInfo.program_category.name%></td>
              <td>
              <% if student.studentInfo.district %>
              <%= student.studentInfo.district.name%>
              <% else %>
              <%= student.studentInfo.city%>
              <% end %>  
              </td>
              <td><%= student.studentInfo.created_at %></td>
            </tr>
            <% end %>
          </tbody>
        </table>

      </div>
      <div class="tab-pane" id="col4TG<%= @user.technicalgroups.index(group) +1 %>">
        <table class="table table-hover">
          <thead>
            <tr>
              <th>Full Name</th>
              <th>Program</th>
              <th>District</th>
              <th>Date</th>
            </tr>
          </thead>
          <tbody>
           <% @user.level_four(@user.technicalgroups.index(group), "technical").each do |student| %>
           <tr>
            <td><%= student.first_name %> <%= student.last_name%></td>
            <td><%= student.studentInfo.program_category.name%></td>
            <td>
            <% if student.studentInfo.district %>
            <%= student.studentInfo.district.name%>
            <% else %>
            <%= student.studentInfo.city%>
            <% end %>  
            </td>
            <td><%= student.studentInfo.created_at %></td>
          </tr>
          <% end %>
        </tbody>
      </table>

    </div>
    <div class="tab-pane" id="col5TG<%= @user.technicalgroups.index(group) +1 %>">
      <table class="table table-hover">
        <thead>
          <tr>
            <th>Full Name</th>
            <th>Program</th>
            <th>District</th>
            <th>Date</th>
          </tr>
        </thead>
        <tbody>
          <% @user.level_five(@user.technicalgroups.index(group), "technical").each do |student| %>
          <tr>
            <td><%= student.first_name %> <%= student.last_name%></td>
            <td><%= student.studentInfo.program_category.name%></td>
            <td>
            <% if student.studentInfo.district %>
            <%= student.studentInfo.district.name%>
            <% else %>
            <%= student.studentInfo.city%>
            <% end %>  
            </td>
            <td><%= student.studentInfo.created_at %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
</div>
<% end %> 